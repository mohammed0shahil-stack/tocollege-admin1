import { ClipboardCheck } from 'lucide-react';
import PlaceholderPage from '../../components/ui/PlaceholderPage';

export default function Admissions() {
  return (
    <PlaceholderPage
      icon={ClipboardCheck}
      title="Admissions"
      description="Application review and admission decisions will live here."
    />
  );
}
