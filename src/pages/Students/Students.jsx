import { Users } from 'lucide-react';
import PlaceholderPage from '../../components/ui/PlaceholderPage';

export default function Students() {
  return (
    <PlaceholderPage
      icon={Users}
      title="Students"
      description="This section isn't part of the current build — it'll follow the same table-and-detail patterns as College Management once it's ready."
    />
  );
}
